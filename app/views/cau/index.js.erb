$('.find-button').bind('ajax:success', function() {
  $('#ajax-information').html("<%= escape_javascript render(partial: 'detail') %>");
  $('#ajax-comments').html("<%= escape_javascript render(partial: 'comments') %>");
});